#Un buon modo per essere sicuro al 100% che .env non finisca mai per sbaglio in un commit: lancia "git status" prima di ogni commit e controlla che .env non compaia nella lista dei file "da aggiungere". Se lo vedi lì, fermati e controlla il .gitignore prima di procedere.

#python-dotenv + load_dotenv() + os.getenv() è il pattern standard per leggere segreti in Python
GROQ_API_KEY=
GOOGLE_API_KEY=